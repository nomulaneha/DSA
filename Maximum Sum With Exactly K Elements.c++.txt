class Solution {
public:
    int maximizeSum(vector<int>& nums, int k) {
        sort(nums.begin(), nums.end());
        int maxi=nums[nums.size()-1];
        int sum=maxi;
        while(k>1){
            maxi++;
            sum+=maxi;
            k--;
        }
        return sum;
    }
};